// src/components/organisms/LoginForm.jsx
import React from "react";
import { Box } from "@mui/material";

const DetailView = ({ children, style }) => {
  return <Box sx={style}>{children}</Box>;
};

export default DetailView;
